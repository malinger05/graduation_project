# Layer 1 — Flask UI + ATM client
Flask>=3.0.0
Flask-WTF>=1.2.0
python-dotenv>=1.0.0
qrcode[pil]>=7.4.2
keyring>=25.0.0
requests>=2.32.0
honcho>=1.1.0

# Layer 2 — middleware (FastAPI)
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.6.0

# Layer 2 — middleware's own database (idempotency, sessions, etc.)
# This DB holds operational state ONLY. Banking data still lives in Core Banking.
SQLAlchemy>=2.0.0
psycopg[binary]>=3.1.0

# Blockchain (used by middleware to log canonical hashes to Sepolia)
web3>=7.0,<8

pyserial>=3.5

pytest-cov>=5.0.0
httpx>=0.27.0
responses>=0.25.0
